# ===========================================
# Rounds — Environment Variables
# ===========================================
# Copy to .env.local and fill in values

# --- Neon Postgres (via Vercel integration) ---
POSTGRES_URL="postgres://..."

# --- Custom Auth (JWT) ---
JWT_SECRET="generate-with-openssl-rand-base64-64"

# --- App Config ---
NEXT_PUBLIC_APP_NAME="Rounds"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# --- GetStream Chat (Milestone 1) ---
NEXT_PUBLIC_GETSTREAM_API_KEY="ekbhy4vctj9g"
GETSTREAM_API_SECRET="your-getstream-api-secret-from-dashboard"

# --- Resend (Phase 2: magic link login) ---
# RESEND_API_KEY="re_..."

# --- Vercel Blob (Phase 2: file uploads) ---
# BLOB_READ_WRITE_TOKEN="..."

# --- Claude API (Phase 4) ---
# ANTHROPIC_API_KEY="sk-ant-..."
